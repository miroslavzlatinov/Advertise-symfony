{% extends 'base.html.twig' %}

{% block body %}
    <h1>Image</h1>
    {% include 'flash/flash.html.twig' %}

<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <div class="panel panel-primary">
                <div class="panel-heading">{{ image.name }}</div>
                <div class="panel-body"><img src="{{ '/images/advert/'~image.location }}" class="img-responsive"
                                             style="width:100%" alt="Image"></div>
                <div class="panel-footer">{{ image.name }}</div>
            </div>
        </div>
    </div>
    </div>
    {#<table>#}
        {#<tbody>#}
            {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ image.id }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Name</th>#}
                {#<td>{{ image.name }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Location</th>#}
                {#<td>{{ image.location }}</td>#}
            {#</tr>#}
        {#</tbody>#}
    {#</table>#}

    <ul>
        <li>
            <a href="{{ path('image_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('image_edit', { 'id': image.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
